# backend/src/main/resources/application.properties

server.port=8085

spring.datasource.url=jdbc:mysql://localhost:3306/smartcampus_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Colombo
spring.datasource.username=root
# In .properties, # starts a comment unless escaped — otherwise the real password is truncated.
spring.datasource.password=Theekshana.12

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (optional) allow bigger payloads if needed later
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=94636995709-ieofrp3uk8pt2oudvjceoc38panekblc.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=profile,email
# backend/src/main/resources/application.properties

